apiVersion: v1
kind: Service
metadata:
  name: {{ include "halcyon-video.fullname" . }}
  labels:
    {{- include "halcyon-video.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "halcyon-video.selectorLabels" . | nindent 4 }}
