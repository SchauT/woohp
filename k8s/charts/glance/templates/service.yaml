apiVersion: v1
kind: Service
metadata:
  name: glance
  namespace: default
spec:
  selector:
    app: glance
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
    name: http
  type: ClusterIP
